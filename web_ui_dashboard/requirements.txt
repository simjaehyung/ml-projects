# ── 웹 서버 ──
flask>=2.3.0
Werkzeug>=2.3.0

# ── 이미지 처리 ──
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# ── 딥러닝 (On-the-fly DataLoader 연동) ──
# torch>=2.0.0         # PyTorch (선택 - 설치 시 주석 해제)
# torchvision>=0.15.0  # torchvision (선택)

# ── 유틸리티 ──
python-dateutil>=2.8.0
